# frozen_string_literal: true
source "https://rubygems.org"

# 使用 github-pages gem,让本地预览的 Jekyll 版本与 GitHub 线上完全一致,
# 避免"本地能跑、线上报错"的问题。
# 说明:GitHub 线上构建自带环境,不读 Gemfile;此文件仅用于本地预览。
gem "github-pages", group: :jekyll_plugins
